services:
  app:
    build: .
    ports:
      - "80:8000"
    volumes:
      - app-data:/app/data
      - app-uploads:/app/uploads
    env_file:
      - backend/.env
    restart: unless-stopped

volumes:
  app-data:
  app-uploads:
